enum Planets {
    Mercury,
    Venus,
    Earth 
}   // enum é um tipo especial de dados que pode conter apenas um conjunto de valores, transforma a string em um numero

Planets.Earth

enum Roles {
    ADMIN,
    USER
}

Roles.ADMIN 
