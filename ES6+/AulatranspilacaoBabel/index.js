function testingES6(test1, test2,) { // nessa funçao ha uma virgula depois do ultimo argumento, portanto outras versões do ES6 não funcionam, para isso, é necessário usar o babel.
    alert(`${test1} ${test2}`);
}